{
  "name": "banshee-ui",
  "license": "MIT",
  "scripts": {
    "build": "esbuild main.jsx --bundle --outfile=dist/bundle.js --format=iife --loader:.jsx=jsx",
    "watch": "esbuild main.jsx --bundle --outfile=dist/bundle.js --format=iife --loader:.jsx=jsx --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
